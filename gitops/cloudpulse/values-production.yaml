# EKS production values — images from ECR (same account as the cluster).
# Apply with:
#   helm upgrade --install cloudpulse ./charts/cloudpulse -n cloudpulse --create-namespace -f gitops/cloudpulse/values-production.yaml

backend:
  enabled: true
  replicaCount: 1
  image:
    repository: durganadimpalli/cloudpulse-api-gateway
    tag: "84b8b1f8f35d"
    pullPolicy: Always
  hpa:
    enabled: false
  env:
    HTTP_ADDR: ":8080"
    ENVIRONMENT: production
    RABBITMQ_URI: "amqp://guest:guest@rabbitmq:5672/"
    TRIP_SERVICE_URL: "trip-service:9093"
    DRIVER_SERVICE_URL: "driver-service:9092"
    DRIVER_SERVICE_HTTP_URL: "http://driver-service:8085"
    TRIP_SERVICE_HTTP_URL: "http://trip-service:8086"
    JAEGER_ENDPOINT: ""
    REDIS_URL: "redis://cloudpulse-production-redis.t8xdwt.ng.0001.use1.cache.amazonaws.com:6379/0"
    JWT_SECRET: "cloudpulse-change-me-in-production"
frontend:
  enabled: true
  replicaCount: 1
  image:
    repository: durganadimpalli/cloudpulse-web
    tag: "84b8b1f8f35d"
    pullPolicy: Always
  env:
    NODE_ENV: production
    NEXT_PUBLIC_API_URL: "https://api.cloudpulse.live"
    NEXT_PUBLIC_WEBSOCKET_URL: "wss://api.cloudpulse.live/ws"
driverFrontend:
  enabled: true
  replicaCount: 1
  image:
    repository: durganadimpalli/cloudpulse-web-driver
    tag: "84b8b1f8f35d"
    pullPolicy: Always
  env:
    NODE_ENV: production
    NEXT_PUBLIC_API_URL: "https://api.cloudpulse.live"
    NEXT_PUBLIC_WEBSOCKET_URL: "wss://api.cloudpulse.live/ws"
ingress:
  enabled: true
  className: alb
  annotations:
    kubernetes.k8s.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    alb.ingress.kubernetes.io/ssl-redirect: "443"
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:633193633631:certificate/2bcfcbce-2048-4124-8993-c3c1519c221c
    alb.ingress.kubernetes.io/success-codes: "200-399"
    # Keep WebSocket connections alive (default 60s idle timeout drops the map's WS)
    alb.ingress.kubernetes.io/load-balancer-attributes: idle_timeout.timeout_seconds=3600
    alb.ingress.kubernetes.io/group.name: cloudpulse
    alb.ingress.kubernetes.io/tags: Environment=production,Project=cloudpulse
  hosts:
    api:
      host: api.cloudpulse.live
      paths:
        - path: /
          pathType: Prefix
    web:
      host: cloudpulse.live
      paths:
        - path: /
          pathType: Prefix
    www:
      enabled: true
      host: www.cloudpulse.live
    driver:
      host: driver.cloudpulse.live
      paths:
        - path: /
          pathType: Prefix
mongo:
  enabled: true
rabbitmq:
  enabled: true
microservices:
  trip-service:
    enabled: true
    image:
      repository: 633193633631.dkr.ecr.us-east-1.amazonaws.com/cloudpulse-trip-service
      tag: "uber-202607191713"
      pullPolicy: Always
  driver-service:
    enabled: true
    httpPort: 8085
    image:
      repository: durganadimpalli/cloudpulse-driver-service
      tag: "84b8b1f8f35d"
      pullPolicy: Always
    env:
      ENVIRONMENT: production
      RABBITMQ_URI: "amqp://guest:guest@rabbitmq:5672/"
      REDIS_URL: "redis://cloudpulse-production-redis.t8xdwt.ng.0001.use1.cache.amazonaws.com:6379/0"
      AUTH_HTTP_ADDR: ":8085"
      JWT_SECRET: "cloudpulse-change-me-in-production"
      LOCAL_SEED_DRIVERS: "false"
      LOCAL_AUTO_ACCEPT: "false"
      LOCAL_SIMULATE_TRIPS: "false"
      TRIP_OFFER_TIMEOUT_SEC: "30"
      JAEGER_ENDPOINT: ""
  payment-service:
    enabled: true
    image:
      repository: 633193633631.dkr.ecr.us-east-1.amazonaws.com/cloudpulse-payment-service
      tag: "34072bac8d49"
      pullPolicy: Always
namespace:
  create: true
  name: cloudpulse
